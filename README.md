# Pomodoro (WIP)

Core timer/data model implemented in `src/lib/pomodoroState.mjs`.

## Implemented in this task
- session state model
- durations + cycle tracking logic
- start/pause/resume/reset/skip/tick transitions
- local persistence helpers (save/load with malformed-data fallback)
- unit tests for core behavior

## Run tests
```bash
npm test
```
